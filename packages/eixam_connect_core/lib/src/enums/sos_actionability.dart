enum SosOriginKind {
  app,
  ownDevice,
  remoteRelay,
  external,
  unknown,
}

enum SosActionability {
  localActionable,
  externalOnly,
  unknown,
}

enum SosDisplaySurface {
  activeAndHistory,
  historyOnly,
  notificationOnly,
  unknown,
}
