name: eixam_connect_sdk_workspace
description: Workspace manifest for shared analysis and tooling in the EIXAM monorepo
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'

dev_dependencies:
  flutter_lints: ^6.0.0
