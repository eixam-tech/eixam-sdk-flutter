# OpenAPI

Keep the HTTP contract under version control here.

Recommended contents:

- `openapi/eixam-sdk.yaml`
- generated artifacts if needed
- changelog/release notes for contract updates

The HTML documentation in `site/` should remain aligned with the OpenAPI contract stored here.
